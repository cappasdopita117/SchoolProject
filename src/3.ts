// Function to get a random number between 1 and 100
const getRandomNumber = (): number => Math.floor(Math.random() * 100) + 1;

// Example of using the function
console.log(`A random number between 1 and 100 is: ${getRandomNumber()}`);
